let s1 = "catbb";
let s2 = "actaa";

function isAnagram(s1, s2) {
    if(s1.length !== s2.length) return false;

    else if(){
        return true;
    }
    else{
        return false;
    }
}

console.log(isAnagram(s1, s2) ? "Anagram" : "Not Anagram")
